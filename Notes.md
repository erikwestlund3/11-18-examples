# Attendance Quiz

```js
const paragraphs = document.querySelectorAll("p");

console.log(Array.from(paragraphs).map((p) => p.innerText));
```

selects any instance of the p tag

SVG - vector representation of the VITE logo

The DOM is a web API used to build websites and is not part of the JavaScript Language

- Can use it on the Node side or the Browser side

Window is the top level global object

- Document is part of the Window object
